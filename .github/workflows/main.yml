name: Rebuild

on:
  schedule:
  - cron: */5 * * * *

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: netlify/actions/build@master
      secrets: ["GITHUB_TOKEN"]
      env:
      - NETLIFY_SIDE_ID: 59ea48a5-7e88-45ea-852d-f6ea878a7fc4
